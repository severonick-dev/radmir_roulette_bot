# Рантайм-зависимости (то, что реально импортируется)
aiogram>=3.13,<4
SQLAlchemy>=2.0
aiosqlite>=0.20
asyncpg>=0.29        # для Postgres на проде (переключается через DATABASE_URL)
Pillow>=10.4
openai>=1.40
python-dotenv>=1.0
aiohttp-socks>=0.9      # прокси для Telegram (если хостинг режет api.telegram.org)
